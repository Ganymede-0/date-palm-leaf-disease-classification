# =============================================================
# Date Palm Leaf Disease Classification — Python dependencies
# Python >= 3.10 recommended
# =============================================================
#
# For GPU training, install a CUDA-matched build of PyTorch from
# https://pytorch.org/get-started/locally/ instead of the generic
# `torch` / `torchvision` lines below.
# =============================================================

# ---- Deep learning ----
torch>=2.1
torchvision>=0.16
timm>=1.0.0

# ---- Imaging ----
Pillow>=10.0.0
pillow-heif>=0.16.0       # required to load .HEIC images from iPhones

# ---- Data / numerics ----
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# ---- Visualisation ----
matplotlib>=3.7
seaborn>=0.13

# ---- Utilities ----
tqdm>=4.66

# ---- Jupyter / notebook runtime ----
# (only needed if you run the .ipynb files locally; Google Colab
# already has these.)
jupyter>=1.0
ipykernel>=6.25
